# Normalise to LF in the repository regardless of contributor platform.
# Workflow `run:` blocks execute on Linux runners, and mixed endings produce
# diffs that are pure noise.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.jar binary
*.class binary
